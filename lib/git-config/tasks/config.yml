---
 - name: Git configure
   become_user: msilveira
   command: "{{ item }}"
   with_items:
     - git config --global user.email "{{ username }}"
     - git config --global user.name "{{ useremail }}"
